### Todo List Backend ###

Простое REST API для управления списком задач (to-do list). Проект создан как учебный пример backend-приложения на
Spring Boot с использованием Jakarta Persistence (JPA), валидации и базы данных PostgreSQL.

## 🚀 Возможности

- Добавление новой задачи
- Получение списка всех задач
- Получение задачи по ID
- Обновление задачи (текст, статус выполнения)
- Удаление задачи

## 🛠 Технологии

- **Java 23**
- **Spring Boot 4.0.1**
- **Spring Web MVC**
- **Spring Data JPA** (с Hibernate)
- **Jakarta Validation** (Bean Validation)
- **Lombok** — для упрощения кода (getter/setter и т.д.)
- **PostgreSQL** — в качестве СУБД
- **Maven** — сборка проекта

## 📦 Запуск проекта

### Требования

- JDK 21+
- Maven
- PostgreSQL (или можно заменить на H2 для тестов)

### Шаги

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/kirill160/todo-backend.git
   cd todo-backend
2. Настройте подключение к БД в application.properties:

   ```
   spring.datasource.url=jdbc:postgresql://localhost:5432/todo_db
   spring.datasource.username=ваш_логин
   spring.datasource.password=ваш_пароль
   ```

3. Соберите и запустите:
   ```bash
    mvn spring-boot:run
   ```
   Приложение будет доступно по адресу: http://localhost:8080

### 📡 Примеры запросов
   ```
   POST /api/tasks
   Content-Type: application/json

   {
   "message": "Выучить Spring Boot",
   "complete": false
   }  
   ```
   Получить все задачи:
   ```
   GET /api/tasks
   ```
   Получить задачу по ID
   ```
   GET /api/tasks/{id}
   ```
   Пример:
   ```
   GET /api/tasks/1
   ```
   Создать новую задачу
   ```
   POST /api/tasks
   Content-Type: application/json
   ```
   Тело запроса:
   ```
   {
  "message": "Выучить Spring Boot",
  "complete": false
   }
   ```
   Обновить задачу
   ```
   PUT /api/tasks/{id}
   ```
   Удалить задачу
   ```
   DELETE /api/tasks/{id}
   ```